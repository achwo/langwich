# paths should lead to the Dockerfile
FRONTEND_PATH=~/path/to/langwich/Dockerfile
BACKEND_PATH=~/path/to/vocapi/Dockerfile

# can be generated with mix phx.gen.secret
SECRET_KEY_BASE=SOME_VERY_LONG_STRING

# db configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

